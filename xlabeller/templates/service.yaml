apiVersion: v1
kind: Service
metadata:
  name: {{ include "xlabeller.fullname" . }}
  labels:
    {{- include "xlabeller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xlabeller.selectorLabels" . | nindent 4 }}
